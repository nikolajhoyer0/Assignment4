-module(erc).
-export([start/0,
         connect/2,
         chat/2,
         history/1,
         filter/3,
         plunk/2,
         censor/2]).


%start().
%connect(Serve, Nick).
%chat(Server, Cont).
%history(Server).
%filter(Sever, Method, P).
%plunk(Server, Nick).
%censor(Server, Words).